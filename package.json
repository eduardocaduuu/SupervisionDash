{
  "name": "supervision-segments",
  "version": "1.0.0",
  "description": "Neo-Brutalist Retro Gamer Dashboard for Dealer Segment Tracking",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node src/index.js",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "render-build": "npm install && cd server && npm install && cd ../client && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
